{
  "name": "kivli-admin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.92.0"
  }
}

