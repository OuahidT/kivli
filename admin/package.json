{
  "name": "kivli-admin",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.6",
    "typescript": "^5.9.2",
    "wrangler": "^4.92.0"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
